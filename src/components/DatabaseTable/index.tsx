import { Header, Table } from './subcomponents';

export const DatabaseTable = () => {
  return (
    <main className="p-6 h-screen overflow-hidden">
      <Header />
      <Table />
    </main>
  );
};
